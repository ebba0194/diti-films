import React from 'react';
import ReactDOM from 'react-dom';

import './styles.css';

const Index = () => (
  <div className="bg-white p-8 container text-center my-8 mx-auto max-w-xl overflow-hidden">
    hjk
  </div>
);

ReactDOM.render(<Index />, document.getElementById('index'));
